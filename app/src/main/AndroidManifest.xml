<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.thoughtworks.retailshop">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="23"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name="com.thoughtworks.retailshop.AppController"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/MyTheme">

        <!-- Single Activity Model -->
        <activity
            android:name="com.thoughtworks.retailshop.view.activities.ECartHomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.thoughtworks.retailshop.view.activities.APrioriResultActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"/>

        <!-- Splash Activity -->
        <activity
            android:name="com.thoughtworks.retailshop.view.activities.SplashActivity"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.thoughtworks.retailshop.view.activities.ShippingAndPayment"
            android:label="@string/title_activity_shipping_and_payment"
            android:theme="@style/MyTheme">
        </activity>
        <activity
            android:name="com.thoughtworks.retailshop.view.activities.ThankYouScreenActivity"
            android:label="@string/title_activity_thank_you_screen"
            android:theme="@style/MyTheme">
        </activity>
    </application>

</manifest>